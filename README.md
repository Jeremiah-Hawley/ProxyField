# ProxyHub
## Description
Takes a moxhub URL and creates a PDF in 8.5x11 format that can be printed for proxying

## TODO - ProxyHub.py
Bug Fixes
 - fix the tokens feature  
- remove vestigial flags (commander, favorites and verbose)

User Input
 - could take plaintext export as input instead of URL 
 - y/n flag to choose with or without a proxy mark / signature

Two sided print
 - Have an option to use a custom back of card (use the "peronal/custom art" flag to activate)

persistent features (optional)
 - stores current collection (CSV maybe?), and has a y/n flag for if it should print new copies, maybe an int input for if it should print cards when you have under an arbitrary number of copies
 - store favorite art of each card and uses that art when printing (makes some procesies easier), also adds custom art functionality
 - take collection input from moxfield

Optional non-persistent features (optional)
 - have a option to only print one side (except on two sided cards)
 - create a drop down menu for input type and have support for more websites and plaintext

 Very Last things
 - create an account on some tipping cite instead of using venmo in the help menu
 - Open Source on GitHub
 - Package as an application that can be downloaded and ran
 - post on r/proxies and other appropriate subreddits


